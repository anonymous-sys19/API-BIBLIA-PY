services:
  - type: web
    name: ghostroot-bible-api
    runtime: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt && cd docs-ui && npm ci --cache $HOME/.npm --prefer-offline && npm run build
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
